1.shell不断地输出 mysh> 注意后面有一个空格
  输出后就等待用户输入命令
  输入命令之后，分析命令，然后执行相应的代码，执行完毕之后回到主函数中
2.处理命令，即处理字符串。endswith记录是否结尾存在 ‘&’， IfRedirection 记录是否需要重定向
3.一些shell命令是系统自带的，通过execvp调用
4.后台进程的实现，在父进程中，不等待子进程，使用数组ChildProcess记录那些需要
  后台运行的子进程的pid，在父进程中，如果键入“wait”命令，则对所有数组中的子进程进行
  等待，调用waitpid函数
5.pipe.c是实现管道的一个样例
  父进程执行完整的语句，而每一条由 ‘|’隔开的子语句通过调用子进程得到中间结果，存放在中间文件中，
  依次执行语句，将中间结果作为下一条子语句的参数输入。




somethingOthers：

input到的buf，记得在最后加一个'\0'
cd 路径不支持中文
pwd,cd由于是和shell进程相关，所以不能放到子进程中执行
pwd ls支持重定向






